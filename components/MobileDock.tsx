import React from 'react';

// This component is no longer used and can be considered deprecated.
// It is kept in the project to avoid breaking file references but has no rendered output.
export const MobileDock: React.FC<{ isInventoryActive: boolean; }> = () => {
    return null; 
};
